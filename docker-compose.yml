version: '3.8'

services:
  dth-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./dth_data:/app/dth.db

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
